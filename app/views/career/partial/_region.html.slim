- regions.each do |region|
  li
    input.square-checkbox name="regions" type="checkbox" value=(region.id) checked=(region.name == @region ? 'checked' : false)
    span.filter-checkbox
    span.careers-filter-details__name
      = region.name